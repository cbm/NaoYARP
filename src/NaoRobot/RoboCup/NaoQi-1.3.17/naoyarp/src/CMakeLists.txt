##############################
#
# WELCOME
#
# This is the main cmake configuration file
# associated with your project
# You can define specific compilation options here
#
# More about cmake : www.cmake.org

# This file was generated by Aldebaran Robotics ModuleGenerator


############################ PROJECT NAME
PROJECT( NAOYARP )




############################ DEFINITION
# Add definitions needed here
ADD_DEFINITIONS( ${ALCOMMON_DEFINITIONS} ${PTHREAD_DEFINITIONS} ${TINYXML_DEFINITIONS} ${BOOST_DEFINITIONS})




############################ INCLUDE DIRECTORY
# Define include directories here
INCLUDE_DIRECTORIES( ${PROXIES_INCLUDE_DIR} ${ALCOMMON_INCLUDE_DIR} ${PTHREAD_INCLUDE_DIR}  ${TINYXML_INCLUDE_DIR}  ${BOOST_INCLUDE_DIR})




############################ PROJECT SOURCES FILES
# Add source files needed to compile this project
SET(NAOYARP_SRCS

#<FILES> don't remove this comment
module.cpp module.h
#</FILES> don't remove this comment

    naoyarp.cpp naoyarp.h
  )



############################ CHECK LIBRARY / EXECUTABLE OPTION

IF( NAOYARP_IS_REMOTE STREQUAL "OFF" )
    MESSAGE( STATUS "naoyarp is compiled as a dynamic object" )
    ADD_LIBRARY( naoyarp SHARED ${NAOYARP_SRCS} )
ELSE( NAOYARP_IS_REMOTE STREQUAL "OFF" )
    MESSAGE( STATUS "naoyarp is compiled as a remote object" )
    ADD_EXECUTABLE( naoyarp ${NAOYARP_SRCS} )
ENDIF( NAOYARP_IS_REMOTE STREQUAL "OFF" )




############################ SET LIBRARIES TO LINK WITH
TARGET_LINK_LIBRARIES( naoyarp ${ALCOMMON_LIBRARIES} ${PTHREAD_LIBRARIES} ${NEWMAT_LIBRARIES}  ${TINYXML_LIBRARIES} ${BOOST_LIBRARIES})

target_link_libraries( naoyarp LibNaoRoboCup LibGeneric)

############################ BINARY COPY
# copy binaries in the output folder automatically
GET_TARGET_PROPERTY( NAOYARP_LOCATION naoyarp LOCATION)


IF ( WIN32 )
    STRING(REGEX REPLACE "naoyarp.dll" "" NAOYARP_LOCATION_SRC ${NAOYARP_LOCATION})
    SET (OUTPUT_ROOT_DIR_LIB ${AL_DIR}/modules/lib/ )
    FILE(TO_NATIVE_PATH ${NAOYARP_LOCATION_SRC} NAOYARP_LOCATION_SRC)
    FILE(TO_NATIVE_PATH ${OUTPUT_ROOT_DIR_LIB} OUTPUT_ROOT_DIR_LIB)
ENDIF ( WIN32 )


IF( NAOYARP_IS_REMOTE STREQUAL "OFF" )

IF (WIN32)
	  ADD_CUSTOM_COMMAND(
	    TARGET naoyarp
	    POST_BUILD
	    COMMAND echo ${NAOYARP_LOCATION_SRC}*
    	    COMMAND echo ${OUTPUT_ROOT_DIR_LIB}
	    COMMAND xcopy  ${NAOYARP_LOCATION_SRC}*.dll ${OUTPUT_ROOT_DIR_LIB} /Y /C
	  )
ELSE (WIN32)

  ADD_CUSTOM_COMMAND(
    TARGET naoyarp
    POST_BUILD
    COMMAND ${CMAKE_COMMAND}
    ARGS -E copy
    ${NAOYARP_LOCATION}
    ${OUTPUT_ROOT_DIR_LIB}
  )
 ENDIF (WIN32)
 ELSE( NAOYARP_IS_REMOTE STREQUAL "OFF" )
  ADD_CUSTOM_COMMAND(
    TARGET naoyarp
    POST_BUILD
    COMMAND ${CMAKE_COMMAND}
    ARGS -E copy
    ${NAOYARP_LOCATION}
    ${OUTPUT_ROOT_DIR_EXE}
  )
ENDIF( NAOYARP_IS_REMOTE STREQUAL "OFF" )



